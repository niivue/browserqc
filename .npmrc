# @niivue/nv-ext-image-processing@rc.10 peer-pins @niivue/niivue@rc.10 while we run
# rc.9. The pin is cosmetic — the conform transform is a pure (hdr,img) worker with
# no NiiVue-internal coupling — so allow the mismatch instead of forcing a NiiVue bump.
legacy-peer-deps=true
